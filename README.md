Powershell script that will toggle Processor Performance Boost mode from Enabled to Disabled, and vice versa.

Note: The script only toggles between enabled and disabled. To add the other modes (Aggressive, Efficient Aggressive, etc...), you would need to edit the script itself.
